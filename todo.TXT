//1.Создаем контроллеру в private секции переменные для передачи в модель.
// Названия переменных класса пишем с начальным подчеркиванием.
// И инициализируем их нулями
//    int _par1 = 0;
//    int _par2 = 0;


//2. Создаем контроллеру функции для получения указателей на эти переменные.
// Указатели нужны чтобы передать их в трекбары и трекбары их уже меняли
//    int* getPar1()
//    int* getPar2()

//3. Чтобы создать несколько трекбаров нужно назначить им разные имена.
// Поэтому добавляем в функцию создания трекбара допольнительно имя
// Имя name и параметр trackbar используем в функции cv::createTrackbar
// Также меняем максимальный диапазон трекбарас 0..100 на 0..255
//void ViewSrc::setTrackBar(cv::String name, int* trackbar)


//4.После создания второго окна, создаем ему два трекбара. В функции setTrackBar отправляем
// два любых разных имени и из контроллера берем два указателя на его переменные
// Трекбары будут заполнять в итоге переменные контроллера
// ViewSrc windSecond("Test_window2", cv::WINDOW_AUTOSIZE);
// windSecond.setTrackbar("par1", controller.getPar1());
// windSecond.setTrackbar("par2", controller.getPar2());

//5.Модель будет использовать параметры из трекбара. Эти параметры хранятся в контроллере.
// Поэтому нужна еще одна функция для передачи параметров контроллера в модель
// Заведем в базовой модели свои переменные int _par1, _par2; Секцию для них зададим protected,
// так как эти переменные будут использовать наследники
//class Model {
//protected:
//    int _par1, _par2;
// И создадим в базовой модели функцию установки параметров
//    void setParams(int par1, int par2)

//6.В контроллере в функции work переправляем параметры контроллера в модель
      ...
      if (_model != 0) {
            _model->setParams(_par1, _par2);
            frameFromModel = _model->process(frame);
      }
      ...

//7. В CannyModel в функцию process первым делом преобразуем frame в серую картинку
// cv::Mat gray;
// cv::cvtColor(frame, gray, cv::COLOR_BGR2GRAY);
// Следом уже вызываем функцию канни с араметрами от трекбаров
// cv::Mat canny;
// сv::Canny(gray, canny, _par1, _par2);